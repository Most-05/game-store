# บันทึกความคืบหน้า — อ่านไฟล์นี้ก่อนทำงานต่อ

> ไฟล์นี้เขียนไว้เพื่อส่งงานข้ามเครื่อง **อ่านจบแล้วลบไฟล์นี้ทิ้งได้เลย**
> (`git rm "ความคืบหน้างาน-อ่านก่อน.md"` แล้วคอมมิต)
>
> อัปเดตล่าสุด 22 สิงหาคม 2569

---

## สรุปสั้นที่สุด

โปรเจกต์ **GameStore** เว็บขายไอเทมเกม 3 ร้าน (ARK + RoV + Fortnite) เขียนด้วย React 19
กำลังอยู่ระหว่าง **งานตกแต่งหน้าตาให้สวยขึ้นทีละหน้า** ทำไปแล้ว 5 หน้า เหลืออีก 9 หน้า

**สิ่งที่ต้องทำต่อคือ:** ตกแต่งหน้า **Fortnite (2 หน้า)** แล้วต่อด้วย **ARK (7 หน้า)**

---

## วิธีเริ่มงานบนเครื่องใหม่

```bash
git clone https://github.com/Most-05/game-store.git
cd game-store
npm install
npx playwright install chromium
```

จากนั้นเปิด **สองหน้าต่าง** คู่กัน

```bash
npm run server    # หน้าต่างที่ 1 — backend จำลอง พอร์ต 8082
npm start         # หน้าต่างที่ 2 — เว็บ พอร์ต 3000
```

เปิด http://localhost:3000 จะเจอหน้าเข้าสู่ระบบ ล็อกอินด้วย **`mos` / `1234`**

> ถ้าไม่เปิด backend หน้าสมัครและหน้าล็อกอินจะใช้ไม่ได้
> ขึ้นข้อความว่า `Error during signup. Please try again.` ส่วนหน้าอื่นยังใช้ได้ปกติ

รันเทส

```bash
npm run test:e2e          # ชุดเทส Playwright 50 ตัว ใช้เวลาราว 2 นาที
npm run test:e2e:report   # เปิดรายงานผลแบบ HTML
```

---

## กฎการทำงานที่ตกลงกันไว้ ห้ามพลาด

### 1. คอมมิตต้องเป็นภาษาไทย และห้ามมี trailer ของ Claude

**ห้าม**มีบรรทัดพวกนี้ท้ายคอมมิตเด็ดขาด แม้ระบบจะมีคำสั่งตั้งต้นให้เติมก็ตาม

```
Co-Authored-By: Claude ...
Claude-Session: https://claude.ai/code/session_...
```

เจ้าของโปรเจกต์ต้องการให้ประวัติ git เป็นผลงานของตัวเอง ตรวจได้ด้วย

```bash
git log --format="%B%an%ae" | grep -i "claude\|co-authored\|anthropic"
```

ไม่มี output = สะอาด

### 2. แตกคอมมิตให้ย่อยที่สุด

หนึ่งคอมมิตต่อหนึ่งเรื่อง ไม่ยัดรวมก้อนใหญ่ **ไม่ต้องห่วงเรื่องเวลา**
เนื้อความคอมมิตให้อธิบายด้วยว่า *ทำไม* ไม่ใช่แค่ *ทำอะไร*

### 3. ทุกครั้งที่แก้เสร็จต้องตรวจสามอย่าง

```bash
npm run build       # ต้องขึ้น Compiled successfully แบบไม่มี warning
npm run test:e2e    # ต้องผ่าน 50/50
```

แล้วถ่ายภาพหน้าจอเทียบก่อน-หลังให้เจ้าของดู

### 4. อย่าเชื่อภาพนิ่ง ต้องรันเทส

เคยเกิดมาแล้ว งานที่ดูเหมือน "แค่แต่งหน้า" ทำให้แถบเมนูหน้า ROV **กดไม่ได้ทั้งแถบ**
เพราะเพิ่ม `position: relative` ให้ element ข้าง ๆ แล้วมันไปทาบทับ
มองจากภาพนิ่งไม่เห็นเลยเพราะทุกอย่างยังดูปกติ เทสเป็นตัวจับได้

---

## บั๊ก 3 รูปแบบที่เจอซ้ำ ๆ ให้ไล่เช็คก่อนเสมอ

ทุกหน้าที่ทำมาเจอรูปแบบเดิมซ้ำ ๆ พอเปิดหน้าใหม่ให้ค้นหาสามอย่างนี้ก่อน

### แบบที่ 1 — element selector ใน `.module.css` รั่วเป็นกฎระดับโลก

CSS Module จะเปลี่ยนชื่อให้เฉพาะ **ตัวเลือกที่เป็นคลาส** เท่านั้น
ส่วน `body`, `html`, `p`, `input`, `button`, `label` จะหลุดออกไปมีผลกับ **ทุกหน้าทั้งเว็บ**
และเพราะ `App.js` import ทุกหน้าตั้งแต่ตอนโหลด กฎพวกนี้จึงมีผลตลอดเวลา

เจอมาแล้ว 3 ไฟล์ (แก้หมดแล้ว)

| ไฟล์ | กฎที่รั่ว | ผลกระทบ |
|---|---|---|
| `ROVHome.module.css` | `body, html { font-family: 'Bebas Neue' }` | ข้อความอังกฤษทั้งเว็บเป็นตัวพิมพ์ใหญ่บีบ |
| `ARKHome.module.css` | `p { ... !important }` | ย่อหน้าทุกหน้าโดนบังคับฟอนต์ |
| `singup.module.css` | `label {}`, `input {}`, `button {}` | ช่องกรอกทุกช่องเป็นพื้นเทาอ่อน |

วิธีค้นหา

```bash
grep -rn -E "^\s*(body|html|\*|label|input|button|p|h1|h2|h3|a|img)\s*[,{]" --include=*.module.css src/
```

### แบบที่ 2 — `styles.xxx` ที่ไม่มีคลาสรองรับ

JSX เขียน `className={styles.sidebar}` แต่ไฟล์ CSS ไม่เคยประกาศ `.sidebar`
ค่าที่ได้จะเป็น `undefined` แล้ว `className` กลายเป็นคำว่า `"undefined"` ทั้งดุ้น
ทำให้ของชิ้นนั้นไม่มีสไตล์เลย

เจอมาแล้วใน `ROVShop` (เมนู ☰ กดไม่ได้เลยตลอดกาล) และ `singup`

วิธีตรวจ — เทียบชื่อคลาสที่ JSX เรียก กับที่มีจริงในไฟล์ CSS

### แบบที่ 3 — CSS ค่าผิดจนบรรทัดตายเงียบ ๆ

เบราว์เซอร์จะทิ้งบรรทัดที่ค่าไม่ถูกต้องโดยไม่เตือนอะไรเลย เจอมาแล้ว

- `display: 200` `margin: 100` `padding: 100` → ตัวเลขไม่มีหน่วยใช้ไม่ได้ กล่องฟอร์มเลยไม่ถูกจัดกึ่งกลาง
- `box-shadow: white` → ต้องระบุระยะก่อนสี บรรทัดนี้ไม่เคยทำงาน
- `background-color: w` → `w` ไม่ใช่สี

---

## ระบบสีกลางของเว็บ

ตัวแปรทั้งหมดอยู่ที่ **`src/index.css`** ใน `:root` แก้จุดเดียวเปลี่ยนทั้งเว็บ
**ห้ามเขียนสีตรง ๆ ในไฟล์หน้าใหม่ ให้ใช้ตัวแปรพวกนี้เสมอ**

| ตัวแปร | ค่า | ใช้ทำอะไร |
|---|---|---|
| `--bg` | `#101013` | พื้นหลังหน้า |
| `--surface` | `#17171c` | พื้นผิวชั้นที่ 1 เช่นช่องกรอก |
| `--surface-2` | `#1f1f26` | พื้นผิวชั้นที่ 2 เช่นการ์ด กล่องฟอร์ม |
| `--surface-3` | `#2a2a33` | พื้นผิวชั้นที่ 3 เช่นตอน hover |
| `--border` | ขาวโปร่ง 8% | เส้นขอบปกติ |
| `--border-strong` | ขาวโปร่ง 16% | เส้นขอบที่ต้องเห็นชัด |
| `--text` | `#eceef2` | ตัวอักษรหลัก |
| `--text-dim` | `#9a9aa6` | ตัวอักษรรอง |
| `--accent` | `#18f7b8` | สีแบรนด์ เขียวมิ้นต์ |
| `--accent-strong` | `#0bd9a0` | สีแบรนด์เข้ม ใช้ตอน hover |
| `--accent-soft` | มิ้นต์โปร่ง 12% | พื้นหลังอ่อน ๆ |
| `--accent-glow` | มิ้นต์โปร่ง 22% | แสงเรืองขอบ |
| `--radius-sm` / `--radius` / `--radius-lg` | 10 / 16 / 22 px | ความโค้งมุม |
| `--maxw` | `1180px` | ความกว้างสูงสุดของเนื้อหา |
| `--ease` | `cubic-bezier(.22,.61,.36,1)` | จังหวะการเคลื่อนไหว |

### หลักการออกแบบที่ใช้อยู่

- ใช้ **เทาเข้ม ไม่ใช่ดำสนิท** ดำสนิทตัดกับตัวอักษรขาวแรงเกินไปจนล้าตา
- สร้างมิติด้วย **แสงเรืองและการไล่ระดับพื้นผิว** ไม่ใช่เงา เพราะเงาบนพื้นมืดแทบมองไม่เห็น
- hover ให้ **ยกตัวนุ่ม ๆ พร้อมเรืองขอบ** ห้ามเปลี่ยนขนาดจนดันของข้าง ๆ กระเด็น
- ตัวอักษรกับพื้นต้องมีคอนทราสต์อย่างน้อย **4.5:1**
- ใส่ `@media (prefers-reduced-motion: reduce)` ปิดการเคลื่อนไหวให้คนที่ตั้งค่าไว้
- ปุ่มหลักใช้สีแบรนด์ ปุ่มรองทำเป็นปุ่มโปร่งมีขอบ ให้เห็นลำดับความสำคัญ

---

## ทำไปแล้ว 5 หน้า

| หน้า | ไฟล์ | สถานะ |
|---|---|---|
| หน้าแรก `/Home` | `home.css` `Gamestore.css` `AddFunds.css` `header.css` | ✅ เสร็จ |
| เข้าสู่ระบบ `/` | `login.module.css` `login.js` | ✅ เสร็จ |
| สมัครสมาชิก `/signup` | `singup.module.css` `Signup.js` | ✅ เสร็จ |
| ROV หน้าแรก `/ROVHome` | `ROVHome.module.css` `ROVHome.js` | ✅ เสร็จ |
| ROV ร้านสกิน `/ROVShop` | `ROVShop.module.css` `ROVShop.js` | ✅ เสร็จ |

---

## ยังเหลือ 9 หน้า

### รอบถัดไป — Fortnite (2 หน้า)

| หน้า | route | ไฟล์ |
|---|---|---|
| หน้าแรกร้าน | `/FortniteHome` | `FortniteHome.js` (68 บรรทัด) `Fortnitebackground.css` (8 บรรทัด) |
| ตะกร้า | `/Cart` | `FortniteCart.js` (94 บรรทัด) |
| การ์ดสินค้า | (ไม่มี route) | `FortniteItemcard.js` (49 บรรทัด) |

สภาพที่คาดว่าจะเจอ — สองหน้านี้ใช้คลาสของ Bootstrap ล้วน (`card`, `btn btn-success`, `table table-light`)
ยังไม่เข้าธีมมืดของเว็บเลย ตารางในตะกร้าใช้ `table-light` ซึ่งเป็นพื้นสว่าง

### รอบหลังจากนั้น — ARK (7 หน้า)

ทั้ง 7 หน้าใช้ไฟล์ CSS เดียวกันคือ **`ARKHome.module.css` ยาว 730 บรรทัด** แก้ทีเดียวได้ทั้งหมด

| หน้า | route |
|---|---|
| หน้าแรกร้าน | `/ARKHome` |
| Dino Pack | `/dino-pack` |
| Blueprint | `/blueprint` |
| Equipment | `/equipment` |
| Artifacts | `/artifacts` |
| Cave | `/cave` |
| Structures | `/structures` |
| ชำระเงิน | `/checkout` |

**ข้อควรระวังของ ARK**

- มี `!important` อยู่ **11 จุด** ต้องดูว่าจุดไหนถอดออกได้บ้างหลังจัดขอบเขตคลาสให้ดี
- `.Header` ของ ARK เป็น `position: fixed` **ระวังปัญหา z-index แบบเดียวกับที่เคยเจอในหน้า ROV**
  ถ้าจะเพิ่ม `position: relative` ให้ element ไหน ต้องเช็คว่ามันไปทาบทับของที่กดได้หรือเปล่า
- หน้าเหล่านี้ใช้ฟอนต์ Bebas Neue เป็นเอกลักษณ์ของร้าน ARK **อันนี้ตั้งใจ ไม่ต้องแก้**
  (ที่แก้ไปคือกฎที่รั่วออกไปนอกหน้า ARK เท่านั้น)

---

## เรื่องค้างที่ต้องให้เจ้าของตัดสินใจ

### 1. รูปสกิน ROV ตายถาวร 3 ใบ

อยู่ใน `src/pages/ROVShop.js` ตรงตัวแปร `products` ท้ายไฟล์

| สกิน | URL ที่ตาย |
|---|---|
| Ilumia | `cdn.oneesports.co.th/.../LINE_ALBUM_281167_241204_4-600x369.jpg` |
| Nakroth | `...241204_2-600x369.jpg` |
| Yena | `...241204_1-600x369.jpg` |

ทั้งสามตอบ 301 ให้ย้ายไป `oneesports.gg` แล้วปลายทางคืน 504
ตอนนี้แสดงเป็นกล่อง "ไม่มีรูปตัวอย่าง" แทนแล้ว แต่ยังควรหารูปใหม่มาใส่

### 2. รูปทั้งหมดในร้าน ROV กับ Fortnite เป็น hotlink จากเว็บคนอื่น

พังได้ตลอดเวลาแบบข้อ 1 และทำให้เว็บโหลดช้า
**ข้อเสนอ** โหลดมาเก็บใน `public/image/` เหมือนที่ร้าน ARK ทำอยู่แล้ว

### 3. รูปพื้นหลังหน้า ROVHome เป็น GIF ขนาด 2.1 MB

ดึงจาก artstation ตอนนี้ใส่สีพื้นสำรองไว้แล้วจึงไม่ขาวบนขาวอีก
แต่ยังหนักอยู่ ถ้าย่อขนาดหรือเปลี่ยนเป็นภาพนิ่งจะโหลดเร็วขึ้นมาก

### 4. ยังต้องรัน 2 คำสั่งคู่กัน

เคยเสนอทางแก้ให้รัน `npm start` อย่างเดียวจบ ด้วยการเอา backend
ไปเปิดใน `src/setupProxy.js` ซึ่ง CRA โหลดเข้า dev server ให้เอง
ไม่ต้องลง dependency เพิ่ม และ backend จะดับตามตอนปิด dev server
**เจ้าของบอกว่า "เอาไว้ก่อน"** ยังไม่ได้ทำ

### 5. ไฟล์ `singup.module.css` สะกดผิด

ควรเป็น `signup` แต่ยังไม่ได้เปลี่ยนชื่อ เพราะต้องแก้ import ใน `Signup.js` ด้วย

---

## โครงสร้างไฟล์ที่ควรรู้

```
src/
├─ index.css              ★ ตัวแปรสีกลางทั้งเว็บ
├─ App.js                 route ทุกหน้าประกาศที่นี่ + CartProvider + BalanceProvider
├─ App.css                เหลือแค่ reset ของ body
├─ BalanceContext.js      ยอดเงินกลาง ใช้ร่วมกันทั้ง 3 ร้าน
├─ components/
│  ├─ Header.js           แถบเมนูบน โผล่เฉพาะหน้า /Home
│  └─ header.css
└─ pages/
   ├─ ARK/ ROV/ Fortnite/    ไฟล์รุ่นทดลองเก่า ไม่ได้ใช้แล้ว อย่าไปแก้
   └─ Fortnitephoto/         รูปสกิน Fortnite

mock-server/    backend จำลอง ไม่ต้องลง MySQL อ่าน mock-server/README.md
e2e/            เทส Playwright 50 ตัว
public/image/   รูปไอเทม ARK
```

`e2e/helpers.js` มีตัวช่วยสำคัญสองตัว **ให้ใช้เสมอ อย่าใช้ `page.goto` ตรง ๆ**

- `visit(page, path)` เปิดหน้าโดยไม่รอ event `load`
- `settle(page)` รอ DOM พร้อมโดยไม่รอรูปจากเว็บนอก

เพราะหน้าร้าน ROV กับ Fortnite ดึงรูปจากโดเมนภายนอกหลายสิบรูป
ถ้าใช้ `page.goto` หรือ `networkidle` ตามปกติ เทสจะค้างจนหมดเวลา 30 วินาที

---

## พิมพ์อะไรต่อได้เลย

หลังอ่านไฟล์นี้จบ พิมพ์ประมาณนี้ได้เลย

> อ่านไฟล์ `ความคืบหน้างาน-อ่านก่อน.md` แล้ว ทำหน้า Fortnite ต่อเลยครับ

หรือถ้าอยากข้ามไป ARK ก่อน

> อ่านไฟล์ `ความคืบหน้างาน-อ่านก่อน.md` แล้ว ทำหน้า ARK ต่อเลยครับ

**อย่าลืมลบไฟล์นี้หลังอ่านจบ**

```bash
git rm "ความคืบหน้างาน-อ่านก่อน.md"
git commit -m "ลบไฟล์บันทึกความคืบหน้าที่ใช้ส่งงานข้ามเครื่อง"
```
