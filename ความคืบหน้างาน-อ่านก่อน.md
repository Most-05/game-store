# บันทึกความคืบหน้า — อ่านไฟล์นี้ก่อนทำงานต่อ

> อัปเดตล่าสุด 24 กันยายน 2569
>
> ไฟล์นี้เป็นบันทึกสถานะปัจจุบันของโปรเจกต์ ไม่ใช่ไฟล์ชั่วคราว
> ให้แก้ให้ตรงกับความจริงทุกครั้งที่ทำงานเสร็จเป็นชุด อย่าปล่อยให้ล้าสมัย

---

## สรุปสั้นที่สุด

โปรเจกต์ **GameStore** เว็บขายไอเทมเกม 3 ร้าน (ARK + RoV + Fortnite) เขียนด้วย React 19

**งานตกแต่งหน้าตาเสร็จครบทั้ง 14 หน้าแล้ว** ไม่มีหน้าไหนค้าง

**สิ่งที่ต้องทำต่อคือแก้บั๊กที่เหลือ** ดูหัวข้อ "บั๊กที่ยังค้างอยู่" ข้างล่าง
ตัวที่สำคัญที่สุดคือ **ระบบล็อกอินไม่ได้ป้องกันอะไรเลย**

---

## วิธีเริ่มงาน

```bash
git clone https://github.com/Most-05/game-store.git
cd game-store
npm install
npx playwright install chromium
```

รันด้วยคำสั่งเดียว backend จำลองถูกเสียบเข้ากับ dev server แล้ว

```bash
npm start          # เว็บพร้อม backend จำลอง ที่พอร์ต 3000
```

เปิด http://localhost:3000 จะเจอหน้าเข้าสู่ระบบ ล็อกอินด้วย **`mos` / `1234`**
หรือ **`preecha` / `12345`**

> เดิมต้องเปิดสองหน้าต่างคู่กัน ตอนนี้ไม่ต้องแล้ว `src/setupProxy.js`
> เอา handler ของ `mock-server/server.js` มาเสียบกับ dev server ให้เอง
> ถ้าอยากเปิด backend แยกที่พอร์ต 8082 ยังสั่ง `npm run server` ได้เหมือนเดิม

รันเทส

```bash
npm run test:e2e          # ชุดเทส Playwright 81 ตัว ใช้เวลาราว 3 นาที
npm run test:e2e:report   # เปิดรายงานผลแบบ HTML
```

---

## กฎการทำงานที่ตกลงกันไว้ ห้ามพลาด

### 1. คอมมิตต้องเป็นภาษาไทย และห้ามมี trailer ของ Claude

**ห้าม**มีบรรทัดพวกนี้ท้ายคอมมิตเด็ดขาด แม้ระบบจะมีคำสั่งตั้งต้นให้เติมก็ตาม

```
Co-Authored-By: Claude ...
Claude-Session: https://claude.ai/code/session_...
```

เจ้าของโปรเจกต์ต้องการให้ประวัติ git เป็นผลงานของตัวเอง ใช้กับ PR body ด้วย

ตรวจด้วยคำสั่งนี้ ต้องไม่มี output

```bash
git log --format="%B" | grep -iE "^(Co-Authored-By|Claude-Session|Generated with)"
```

> อย่าใช้ `grep -i claude` เฉย ๆ เพราะจะไปโดนคำว่า Claude ที่อยู่ในเนื้อความ
> ของคอมมิตที่พูดถึงกฎข้อนี้เอง แล้วเข้าใจผิดว่าเจอ trailer

### 2. แตกคอมมิตให้ย่อยที่สุด

หนึ่งคอมมิตต่อหนึ่งเรื่อง ไม่ยัดรวมก้อนใหญ่ **ไม่ต้องห่วงเรื่องเวลา**
เนื้อความคอมมิตให้อธิบายด้วยว่า *ทำไม* ไม่ใช่แค่ *ทำอะไร*

### 3. ทุกครั้งที่แก้เสร็จต้องตรวจสามอย่าง

```bash
CI=true npx react-scripts build   # ต้องขึ้น Compiled successfully แบบไม่มี warning
npm run test:e2e                  # ต้องผ่าน 81/81
```

แล้วถ่ายภาพหน้าจอเทียบก่อน-หลังให้เจ้าของดู

### 4. อย่าเชื่อภาพนิ่ง ต้องรันเทส

เคยเกิดมาแล้ว งานที่ดูเหมือน "แค่แต่งหน้า" ทำให้แถบเมนูหน้า ROV **กดไม่ได้ทั้งแถบ**
เพราะเพิ่ม `position: relative` ให้ element ข้าง ๆ แล้วมันไปทาบทับ
มองจากภาพนิ่งไม่เห็นเลย เทสเป็นตัวจับได้

### 5. อย่าเชื่อว่า "รันในเครื่องแล้วผ่าน" แปลว่าใช้ได้จริง

วินโดวส์ไม่สนตัวพิมพ์ใหญ่เล็กในชื่อไฟล์ แต่เซิร์ฟเวอร์ Linux สน
เคยมีบั๊กที่รูปในหน้าชำระเงินจะพัง 19 จาก 26 รายการทันทีที่ deploy
โดยที่ในเครื่องไม่มีวันเห็นอาการ ตอนนี้มีเทสคุมไว้แล้ว

---

## 🔴 บั๊กที่ยังค้างอยู่ — เรียงตามความสำคัญ

### 1. ระบบล็อกอินไม่ได้ป้องกันอะไรเลย  ← สำคัญที่สุด

`src/pages/login.js` บรรทัดที่ 31 เก็บ `access_token` ลง localStorage
หลังล็อกอินสำเร็จ แต่ค้นทั้งโปรเจกต์แล้ว **ไม่มีโค้ดที่ไหนอ่านค่านั้นเลยสักบรรทัด**

```bash
grep -rn "getItem('access_token')" src/     # ไม่มี output
```

และ `src/App.js` ไม่มี route guard ใด ๆ ทุก route ประกาศเป็น `<Route>` ธรรมดาหมด

**อาการ** พิมพ์ URL เข้าไปตรง ๆ ได้เลยโดยไม่ต้องล็อกอิน `/Home`, `/ARKHome`,
`/checkout` เข้าได้หมด และซื้อของได้ด้วย หน้าล็อกอินเป็นแค่ฉากที่กดผ่านไป

**แนวทางแก้** ทำคอมโพเนนต์ครอบ route ที่ต้องล็อกอินก่อน เช็คว่ามี `access_token`
ใน localStorage ไหม ถ้าไม่มีให้ `<Navigate to="/" replace />`
แล้วเพิ่มปุ่มออกจากระบบที่ล้าง token ทิ้ง พร้อมเทสว่าเข้าหน้าที่ต้องล็อกอิน
โดยไม่มี token แล้วต้องโดนเด้งกลับ

**สถานะ** เจ้าของสั่งว่า "เอาไว้ก่อน ค่อยทำ" เมื่อ 23 ส.ค. 2569

### 2. รหัสผ่านเก็บด้วย MD5 เปล่า ๆ ไม่มี salt

`mock-server/server.js` บรรทัดที่ 16

```js
const md5 = (s) => crypto.createHash('md5').update(String(s)).digest('hex');
```

MD5 ออกแบบมาให้เร็ว ซึ่งเป็นสิ่งที่ไม่ควรมีในการแฮชรหัสผ่าน แตกด้วย
rainbow table ได้ในไม่กี่วินาที รหัส `1234` กับ `12345` ที่เป็นบัญชีตั้งต้น
เดาออกทันที

**ข้อควรระวังก่อนแก้** mock ตัวนี้เลียนแบบ backend จริงที่เป็น PHP + MySQL
ถ้าแก้แค่ฝั่ง mock ให้ใช้ bcrypt จะล็อกอินไม่ตรงกับของจริงทันที
ต้องแก้ทั้งสองฝั่งพร้อมกัน หรือทำเป็นรองรับทั้งสองแบบระหว่างช่วงเปลี่ยนผ่าน

**สถานะ** เจ้าของสั่งว่า "เอาไว้ก่อน ค่อยทำ" เมื่อ 23 ส.ค. 2569

### 3. สกินสามใบถูกเอาออกเพราะหารูปไม่ได้ (✅ แก้ไขแล้ว)

Ilumia, Nakroth และ Yena ตอนแรกถูกลบออกจากร้านเมื่อ 23 ส.ค. 2569 เนื่องจากลิงก์รูปเสีย
แต่ปัจจุบัน (ก.ย. 2569) ได้ทำการสร้างรูปภาพใหม่ด้วย AI แบบไม่ติดลิขสิทธิ์ และนำไปวางที่ `public/image/rov/` พร้อมกับเพิ่มรายการกลับเข้าสู่ `ROVShop.js` เรียบร้อยแล้ว

### 4. เรื่องเล็ก ๆ ที่ยังไม่ได้ทำ (✅ แก้ไขแล้ว)

- **รูปสินค้าหน้า Structures กับ Equipment เป็นภาพพื้นสว่าง:** ได้ทำการเปลี่ยนรูปภาพใหม่ (Generate ด้วย AI) เพื่อเอาพื้นหลังสีขาวออก ทำให้ภาพกลมกลืนกับธีมมืดของหน้าเว็บเรียบร้อยแล้ว
- **ยังไม่มีเทสระดับ unit:** ตอนนี้มีการตั้งค่า Jest (รวมถึงปรับแก้ให้อ่าน React Router v7 ได้) และเพิ่ม Unit Test ครอบคลุมการทำงานของ Context (`BalanceContext.test.js`) และหน้า UI (`Header.test.js`) แล้ว

---

## สิ่งที่ทำเสร็จไปแล้ว

### งานตกแต่ง ครบ 14 หน้า

| ร้าน | หน้า | สถานะ |
|---|---|---|
| ทั่วไป | หน้าแรก, เข้าสู่ระบบ, สมัครสมาชิก | ✅ |
| RoV | หน้าแรกร้าน, ร้านสกิน | ✅ |
| Fortnite | หน้าแรกร้าน, ตะกร้า | ✅ |
| ARK | หน้าแรกร้าน + 6 หมวด + หน้าชำระเงิน | ✅ |

### บั๊กสำคัญที่แก้ไปแล้ว (ดูรายละเอียดในประวัติคอมมิต)

- **ช่อง Input ในหน้า Login/Signup แสดงผลผิดเพี้ยน:** โดนสไตล์ `input` จาก `index.css` ทับ ทำให้เห็นแค่เส้นบางๆ (แก้โดยใช้ Specificity ใน CSS Module)
- หน้า Structures แสดงราคา 10000 แต่หักเงินจริง 45000 — แก้ให้อ่านจากตัวเลขเดียวกัน
- หน้าชำระเงินเดาชื่อไฟล์รูปจากชื่อสินค้า จะพัง 19 จาก 26 รายการบนโฮสต์ Linux
- `bootstrap.min.css` โหลดหลัง `index.css` แล้วทับ `body` ทั้งเว็บ พื้นหลังจริง
  เป็นสีขาวมาตลอด
- หน้า Artifacts อ้างคลาสที่ไม่มีอยู่จริง 14 จุด ทำให้ทั้งหน้าไม่มีสไตล์
- หน้า Artifacts ฝังยอดเงิน 10000 ไว้ตายตัว หน้า Structures ไม่แสดงยอดเลย
- การ์ดสินค้า ARK ทั้ง 6 หน้ากดด้วยคีย์บอร์ดไม่ได้
- ปุ่มจ่ายเงินสั่ง `outline: none` ทับกรอบโฟกัส
- รูป 19 ใบดึงจากเว็บคนอื่นรวม 8.4 MB ย้ายมาเก็บเองเหลือ 1.25 MB
- ต้องเปิดสองหน้าต่างรัน เหลือคำสั่งเดียว
- ลบโค้ดรุ่นทดลองที่ไม่มีใคร import ทิ้ง รวมรูปซ้ำ 1.4 MB
- แถบเมนูร้าน ARK ตัวหนังสือโดนตัดบนจอมือถือ
- หน้า 404 แสดง URL ภาษาไทยเป็นรหัส %E0%B8... อ่านไม่ออก

### จัดระเบียบส่วนของร้าน ROV (23 ส.ค. 2569)

เจ้าของรับผิดชอบส่วนนี้เอง จึงเก็บกวาดเฉพาะไฟล์ของร้าน ROV

- `ROVHome.module.css` จาก 331 บรรทัดเหลือ 146 ประกาศคลาสไว้ 16 ตัว
  แต่ใช้จริง 3 ตัว ที่เหลือเป็นสไตล์ของหน้าร้านที่ก๊อปมาวางซ้ำ
- ราคาสินค้าเปลี่ยนจากข้อความ "฿9,900" เป็นตัวเลขล้วน แล้วจัดรูปแบบ
  ตอนแสดงผลที่เดียว แก้ปัญหาที่หน้าเว็บแสดง ฿9,900 ปนกับ ฿7900
  และเลิกแกะข้อความกลับเป็นตัวเลขด้วย regex ตอนหักเงิน
- ย้ายรายการสินค้าขึ้นบนคอมโพเนนต์ เรียงตาม id
- เปลี่ยนชื่อคอมโพเนนต์ `Home` เป็น `ROVHome` เดิมชนกับ `pages/Home.js`
- การ์ดสกินกับปุ่ม ☰ กดด้วยคีย์บอร์ดได้แล้ว
- ปิดป๊อปอัพและเมนูสไลด์ด้วยปุ่ม Esc หรือกดพื้นหลังนอกกล่อง
- เพิ่มไฟล์เทสของร้าน ROV โดยเฉพาะ

### จัดระเบียบร้าน ROV รอบสอง (23 ส.ค. 2569)

- เมนูสไลด์กับปุ่ม ☰ ยังใช้ชุดสีของธีมเก่า คือน้ำเงินเทา #2c3e50 กับไฮไลต์
  สีทอง #ffd700 ซึ่งไม่อยู่ในชุดสีของเว็บเลย เปลี่ยนมาใช้ตัวแปรกลางแล้ว
- ปุ่มปิดเมนูเป็นสีม่วงไม่ว่าจะตั้งสีเป็นอะไร เพราะอักขระ ✖ (U+2716)
  ถูกเบราว์เซอร์เรนเดอร์เป็นอิโมจิซึ่งพกสีมาเอง เปลี่ยนเป็น ✕ (U+2715)
- กล่องยืนยันการซื้อไม่ย้ายโฟกัสเข้าไปในกล่อง กด Tab แล้วหลุดไปการ์ดข้างหลัง
  ตอนนี้มี role="dialog", ย้ายโฟกัสเข้า, ขัง Tab ไว้ในกล่อง และคืนโฟกัสตอนปิด
- เลิกใช้ alert ของเบราว์เซอร์ แจ้งผลในกล่องเดิม และกรณีเงินไม่พอมีปุ่มไปเติมเงิน
- เปลี่ยนชื่อคลาสจาก kebab-case เป็น camelCase ให้ตรงกับทุกไฟล์ในโปรเจกต์
- ตั้งชื่อสกิน Airi สองใบให้ต่างกัน และแปลหัวข้อหน้าร้านเป็นไทย

---

## บั๊ก 3 รูปแบบที่เจอซ้ำ ๆ ให้ไล่เช็คก่อนเสมอ

### แบบที่ 1 — element selector ใน `.module.css` รั่วเป็นกฎระดับโลก

CSS Module จะเปลี่ยนชื่อให้เฉพาะ **ตัวเลือกที่เป็นคลาส** เท่านั้น
ส่วน `body`, `html`, `p`, `input`, `button` จะหลุดออกไปมีผลกับ **ทุกหน้าทั้งเว็บ**
และเพราะ `App.js` import ทุกหน้าตั้งแต่ตอนโหลด กฎพวกนี้จึงมีผลตลอดเวลา

เจอมาแล้ว 4 ไฟล์ แก้หมดแล้ว วิธีค้นหา

```bash
grep -rn -E "^\s*(body|html|\*|label|input|button|p|h1|h2|h3|a|img)\s*[,{]" --include=*.module.css src/
```

### แบบที่ 2 — `styles.xxx` ที่ไม่มีคลาสรองรับ

JSX เขียน `className={styles.sidebar}` แต่ไฟล์ CSS ไม่เคยประกาศ `.sidebar`
ค่าที่ได้เป็น `undefined` แล้ว `className` กลายเป็นคำว่า `"undefined"` ทั้งดุ้น

เจอมาแล้วใน `ROVShop`, `singup` และหนักสุดคือ `ArtifactsPage` ที่มีถึง 14 จุด
ปัจจุบันไม่เหลือแล้ว มีสคริปต์ไล่เช็คอยู่ในประวัติคอมมิต

### แบบที่ 3 — CSS ค่าผิดจนบรรทัดตายเงียบ ๆ

เบราว์เซอร์ทิ้งบรรทัดที่ค่าไม่ถูกต้องโดยไม่เตือนอะไรเลย เจอมาแล้ว
`display: 200`, `margin: 100` (ตัวเลขไม่มีหน่วย), `box-shadow: white`
(ต้องระบุระยะก่อนสี), `background-color: w`

---

## ระบบสีกลางของเว็บ

ตัวแปรทั้งหมดอยู่ที่ **`src/index.css`** ใน `:root` แก้จุดเดียวเปลี่ยนทั้งเว็บ
**ห้ามเขียนสีตรง ๆ ในไฟล์หน้าใหม่ ให้ใช้ตัวแปรพวกนี้เสมอ**

| ตัวแปร | ค่า | ใช้ทำอะไร |
|---|---|---|
| `--bg` | `#101013` | พื้นหลังหน้า |
| `--surface` / `--surface-2` / `--surface-3` | `#17171c` / `#1f1f26` / `#2a2a33` | พื้นผิวสามชั้น |
| `--border` / `--border-strong` | ขาวโปร่ง 8% / 16% | เส้นขอบ |
| `--text` / `--text-dim` | `#eceef2` / `#9a9aa6` | ตัวอักษรหลัก / รอง |
| `--accent` / `--accent-strong` | `#18f7b8` / `#0bd9a0` | สีแบรนด์ เขียวมิ้นต์ |
| `--accent-soft` / `--accent-glow` | มิ้นต์โปร่ง 12% / 22% | พื้นอ่อน / แสงเรือง |
| `--radius-sm` / `--radius` / `--radius-lg` | 10 / 16 / 22 px | ความโค้งมุม |
| `--maxw` | `1180px` | ความกว้างสูงสุดของเนื้อหา |
| `--ease` | `cubic-bezier(.22,.61,.36,1)` | จังหวะการเคลื่อนไหว |

### หลักการออกแบบที่ใช้อยู่

- ใช้ **เทาเข้ม ไม่ใช่ดำสนิท** ดำสนิทตัดกับตัวอักษรขาวแรงเกินไปจนล้าตา
- สร้างมิติด้วย **แสงเรืองและการไล่ระดับพื้นผิว** ไม่ใช่เงา เพราะเงาบนพื้นมืด
  แทบมองไม่เห็น
- hover ให้ **ยกตัวขึ้นแนวตั้งพร้อมเรืองขอบ** ห้ามใช้ `scale` เพราะมันขยาย
  ออกทุกทิศแล้วไปดันของข้าง ๆ กระเด็น
- ตัวอักษรกับพื้นต้องมีคอนทราสต์อย่างน้อย **4.5:1**
- ใส่ `@media (prefers-reduced-motion: reduce)` ปิดการเคลื่อนไหวเสมอ
- ปุ่มหลักใช้สีแบรนด์ ปุ่มรองทำเป็นปุ่มโปร่งมีขอบ ให้เห็นลำดับความสำคัญ
- ฟอนต์ Bebas Neue ใช้เฉพาะร้าน ARK เป็นเอกลักษณ์ที่ตั้งใจ ไม่ต้องแก้

---

## โครงสร้างไฟล์ที่ควรรู้

```
src/
├─ index.js               ★ ลำดับ import ของ bootstrap กับ index.css ห้ามสลับ
├─ index.css              ★ ตัวแปรสีกลางทั้งเว็บ
├─ setupProxy.js          ★ เสียบ mock backend เข้า dev server (CRA โหลดเอง)
├─ App.js                 route ทุกหน้า + CartProvider + BalanceProvider
├─ BalanceContext.js      ยอดเงินกลาง ใช้ร่วมกันทั้ง 3 ร้าน
├─ components/Header.js   แถบเมนูบน โผล่เฉพาะหน้า /Home
└─ pages/
   ├─ ARKHome.module.css     สไตล์ร่วมของร้าน ARK ทั้ง 8 หน้า
   ├─ arkCardProps.js        ทำให้การ์ด ARK กดด้วยคีย์บอร์ดได้
   ├─ FortniteShop.module.css  สไตล์ร่วมของร้าน Fortnite ทั้ง 3 ส่วน
   ├─ ROVShop.module.css / ROVHome.module.css
   ├─ rov-home-bg.jpg        พื้นหลัง ROVHome (ต้องอยู่ใน src ไม่ใช่ public)
   └─ Fortnitephoto/         รูปสกิน Fortnite

public/image/          รูป ARK
public/image/rov/      รูปสกิน ROV (ย้ายมาจากเว็บนอกแล้ว)
public/image/games/    รูปการ์ดเกมในหน้า /game
mock-server/           backend จำลอง ไม่ต้องลง MySQL
e2e/                   เทส Playwright 81 ตัว
```

`e2e/helpers.js` มีตัวช่วยสำคัญสองตัว **ให้ใช้เสมอ อย่าใช้ `page.goto` ตรง ๆ**

- `visit(page, path)` เปิดหน้าโดยไม่รอ event `load`
- `settle(page)` รอ DOM พร้อมโดยไม่รอรูป

---

## พิมพ์อะไรต่อได้เลย

> อ่าน `ความคืบหน้างาน-อ่านก่อน.md` แล้ว ทำบั๊กข้อ 1 เรื่องระบบล็อกอินต่อเลยครับ

หรือ

> อ่าน `ความคืบหน้างาน-อ่านก่อน.md` แล้ว ทำบั๊กข้อ 2 เรื่องรหัสผ่าน MD5 ต่อเลยครับ
